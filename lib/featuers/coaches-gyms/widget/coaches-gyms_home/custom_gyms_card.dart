import 'package:flutter/material.dart';

class CustomGymCard extends StatelessWidget {
  const CustomGymCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
