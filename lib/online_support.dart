import 'package:flutter/material.dart';

class OnlineSuport extends StatelessWidget {
  const OnlineSuport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
